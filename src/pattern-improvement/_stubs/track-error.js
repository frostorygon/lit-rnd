/**
 * Stub: trackError()
 * Simulates analytics tracking for demo purposes.
 * Logs to console so we can verify it's called correctly.
 *
 * @param {string} category
 * @param {string} message
 */
export function trackError(category, message) {
  console.log(`[trackError] ${category}: ${message}`);
}
